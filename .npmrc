legacy-peer-deps=true
save-exact=false
package-lock=true
optional=true
# Ensure platform-specific binaries are installed
target_platform=linux
target_arch=x64
optional=true
production=false
cache=/tmp/.npm
